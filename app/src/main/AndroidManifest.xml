<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techpaliyal.motionlayoutsamples">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MotionLayoutSamples">
        <activity android:name=".NestedScrollAnimation"></activity>
        <activity android:name=".CustomAnimationActivity" />
        <activity android:name=".PathAnimationActivity" />
        <activity android:name=".PositionAnimationActivity" />
        <activity android:name=".ScaleAnimationActivity" />
        <activity android:name=".SwipeToChangeColorActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PathRotateAnimationActivity" />
        <activity android:name=".SolarSystemActivity" />
    </application>

</manifest>